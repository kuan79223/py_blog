version: '3'
services:
  # 服務的名字
  app:
    # 該路徑底下需要包含Dockerfile
    build: .
    # 對應哪個port
    ports:
      - "8082:8082"
    environment:
    - DB_HOST=db
    - DB_NAME=blog_db
    - DB_USER=blogapp
    - DB_PASSWORD=KBaM534TMpUWTEFU
    depends_on:
    - db
  db:
    image: postgres
    restart: always
    ports: 
      - "5432:5432"
    environment:
      - POSTGRES_USER=blogapp
      - POSTGRES_PASSWORD=KBaM534TMpUWTEFU
      - POSTGRES_DB=blog_db
